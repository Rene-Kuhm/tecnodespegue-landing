---
import Icon from './Icon.astro';
import CompileBadge from './CompileBadge.astro';
import { typeWriter } from '../scripts/heroScript';
---

<section class="relative min-h-screen flex items-center pt-20 overflow-hidden">
  <div class="absolute inset-0 grid-bg opacity-30"></div>
  <div class="absolute inset-0 hero-gradient"></div>
  
  <style>
  .terminal-snippet {
    position: relative;
    margin-top: 1.5rem;
    padding: 1rem;
    background: rgba(0, 0, 0, 0.8);
    border: 1px solid rgba(34, 197, 94, 0.3);
    border-radius: 0.5rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: rgb(34, 197, 94);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  }

  .terminal-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
    padding-bottom: 0.25rem;
    border-bottom: 1px solid rgba(34, 197, 94, 0.2);
  }

  .terminal-dots {
    display: flex;
    gap: 0.25rem;
  }

  .dot {
    width: 0.5rem;
    height: 0.5rem;
    border-radius: 50%;
  }

  .red { background-color: #ef4444; }
  .yellow { background-color: #eab308; }
  .green { background-color: #22c55e; }

  .terminal-title {
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.6875rem;
    font-weight: 500;
  }

  .terminal-body {
    line-height: 1.5;
  }

  .line {
    display: flex;
    align-items: center;
  }

  .line-number {
    display: inline-block;
    width: 2rem;
    color: rgba(34, 197, 94, 0.5);
    text-align: right;
    margin-right: 0.5rem;
    user-select: none;
  }

  .line-content {
    flex: 1;
  }

  .typewriter-text {
    display: inline-block;
  }

  .cursor {
    display: inline-block;
    margin-left: 0.25rem;
    animation: blink 1s step-end infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }
  </style>
  
  <div class="max-w-7xl mx-auto px-8 relative z-10 grid lg:grid-cols-2 gap-12 items-center">
    <div class="space-y-8">
  <CompileBadge />
  
  <div class="terminal-snippet">
    <div class="terminal-header">
      <div class="terminal-dots">
        <div class="dot red"></div>
        <div class="dot yellow"></div>
        <div class="dot green"></div>
      </div>
      <span class="terminal-title">astro.config.ts</span>
    </div>
    <div class="terminal-body">
      <div class="line">
        <span class="line-number">1</span>
        <span class="line-content">import { BlogPost } from '../content/posts'</span>
      </div>
      <div class="line">
        <span class="line-number">2</span>
        <span class="line-content">import Contact from '../components/Contact.astro'</span>
      </div>
      <div class="line">
        <span class="line-number">3</span>
        <span class="line-content">import { CompileBadge } from '../components/CompileBadge.astro'</span>
      </div>
    </div>
  </div>
      
  <h1 class="font-headline text-[2.5rem] md:text-[3.5rem] lg:text-[4.5rem] leading-[1.05] tracking-tight font-bold text-on-surface">
    <span class="typewriter-text">Automatizamos tu </span><span class="text-primary typewriter-text">futuro digital</span>
    <span class="cursor">|</span>
  </h1>
      
      <p class="text-on-surface-variant text-lg max-w-lg font-body leading-relaxed">
        Desarrollo fullstack, automatizaciones con IA y flujos de trabajo inteligentes para empresas que buscan escalar sin límites.
      </p>
      
      <div class="flex flex-wrap gap-4 pt-4">
        <a href="#contacto" class="btn-primary">
          Iniciá tu Proyecto
        </a>
        <a href="#soluciones" class="btn-secondary">
          Nuestros Servicios
        </a>
      </div>
    </div>
    
    <div class="relative hidden lg:block">
      <div class="aspect-square relative">
        <!-- Animated gradient glow -->
        <div class="absolute inset-0 bg-primary/10 blur-[100px] rounded-full animate-pulse-slow"></div>
        
        <!-- 3D-style geometric visualization -->
        <div class="w-full h-full relative rounded-card border border-white/10 bg-gradient-to-br from-surface-container-low to-black overflow-hidden">
          <!-- Grid overlay -->
          <div class="absolute inset-0 grid-bg opacity-20"></div>
          
          <!-- Floating geometric shapes -->
          <div class="absolute inset-0 flex items-center justify-center">
            <div class="relative w-64 h-64">
              <!-- Main sphere -->
              <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-40 h-40 rounded-full bg-gradient-to-br from-primary/20 to-secondary/10 border border-primary/30 animate-float"></div>
              
              <!-- Orbiting rings -->
              <div class="absolute inset-0 border border-primary/20 rounded-full animate-spin-slow"></div>
              <div class="absolute inset-4 border border-secondary/20 rounded-full animate-spin-reverse"></div>
              
              <!-- Corner accents -->
              <div class="absolute top-0 right-0 w-16 h-16 border-t-2 border-r-2 border-primary/40"></div>
              <div class="absolute bottom-0 left-0 w-16 h-16 border-b-2 border-l-2 border-secondary/40"></div>
            </div>
          </div>
          
          <!-- Scan line effect -->
          <div class="absolute inset-0 bg-gradient-to-b from-transparent via-primary/5 to-transparent h-32 animate-scan"></div>
        </div>
        
        <!-- Floating badge with icon -->
        <div class="absolute -bottom-6 -left-6 bg-surface-container p-6 border border-white/5 shadow-2xl rounded-card max-w-xs glass-effect hover:scale-105 transition-transform">
          <Icon name="terminal" class="w-10 h-10 text-secondary mb-4 block" />
          <p class="text-sm font-label text-on-surface-variant leading-snug italic">
            "Transformamos ideas complejas en código de alto rendimiento."
          </p>
        </div>
      </div>
    </div>
  </div>
  <script>
  // Run typewriter effect after a short delay
  document.addEventListener('DOMContentLoaded', () => {
    setTimeout(() => {
      const textElement = document.querySelector('.typewriter-text');
      if (textElement) {
        typeWriter(textElement, 'Automatizamos tu futuro digital', () => {
          // Cursor blink animation is handled by CSS
        });
      }
    }, 300);
  });
  </script>
</section>
